# rNitro v1.2 — Trust & smoothness

Current shipping baseline:

- Stable: `v1.1.0-Final-Reloaded`
- Beta: `v1.2.1`

## Theme

**Install without fear, run quietly, update cleanly.**

## Target version names

| Channel | Tag |
|---------|-----|
| Stable | `v1.2.0` |
| Beta | `v1.2.1-beta` |

Drop multi-word “Reloaded” from *ids* next cycle; keep the story in release notes if desired.

## Must-ship

1. **Notarization** (stable App ZIP + PKG) — Apple Developer ID  
2. **`python3 do_release.py ship`** — sync → build → assert → GitHub → Netlify  
3. **Push `main` README** with every release (Releases alone are not enough)  
4. **Promote to stable:** first-launch tips + AES key storage (keep multi-provider AI on beta)  
5. **Idle defaults + menubar density presets** (Compact / Comfortable / Full)

## Should-ship

- Homebrew cask bump for `v1.2.0`
- GitHub Releases as binary CDN; Netlify hosts site + `version.json`
- In-app short “What’s new”
- Privacy page (live: `/privacy.html`)

## Later

- Linux v0.2 or de-emphasize on landing page  
- Widgets (macOS 14+)  
- Single Swift tree with feature flags  

## Release command (after version.json bump)

```bash
python3 do_release.py ship
# then:
git add -u README.md version.json index.html install-rNitro.sh
git commit -m "release v1.2.0"
git push origin main
```

## Checklist

- [ ] `version.json` ids + artifact names  
- [ ] `python3 do_release.py build` (or `ship`)  
- [ ] `python3 assert_release_versions.py` green  
- [ ] Live `version.json` + unpacked ZIP plists match  
- [ ] GitHub latest tag + README install curl match  
- [ ] Smoke install App ZIP on a clean user account  
